server {
    listen 80;
    server_name _;

    location = /api/phones {
        root /usr/share/nginx/html;
        try_files /phones.json =404;
        default_type application/json;
        add_header Cache-Control "no-store" always;
    }

    location / {
        return 404;
    }
}
